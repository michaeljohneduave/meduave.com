---
const { name, description, status, logo } = Astro.props;
---

<div
  class="p-4 hover:cursor-pointer hover:bg-white hover:bg-opacity-5 rounded-md"
>
  <div>
    <img class="border-0 h-20 w-20 object-cover" src={logo} alt={name} />
  </div>
  <div class="text-3xl">
    <span>{name}</span>
  </div>
  <p>{description}</p>
  <span>Status: <span class="text-slate-300">{status}</span></span>
</div>
